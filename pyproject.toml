[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.optional-dependencies]
docs = [
    "griffe",
    "mkdocs",
    "mkdocs-material",
    "mkdocstrings[python]",
    "pymdown-extensions",
]
tests = [
    "pytest",
]
utils = [
    "mpmath",
]
dev = [
    "qmax[docs,tests,utils]",
    "ruff",
    "prek",
    "pyright",
]

[project]
authors = [
    {name = "Ahmed Allibhoy"}
]
name = "qmax"
readme = "README.md"
requires-python = ">=3.14"
version = "0.01"
dependencies = [
    "jax",
    "jaxtyping",
    "equinox",
    "lineax",
    "numpy",
    "matplotlib",
    "tqdm",
]

[tool.ruff]
target-version = "py314"
exclude = ["tests/", "benchmarks/", "utils/"]
extend-exclude = ["*.ipynb"]
line-length = 100

[tool.ruff.lint]
fixable = ["I001", "F401", "F541"]
ignore = ["B008", "E402", "E731", "E741", "F403"]
select = ["E", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["E501", "F401"]

[tool.pyright]
include = ["qmax"]
reportIncompatibleVariableOverride = false # Incompatible with eqx.AbstractVar
reportUnnecessaryTypeIgnoreComment = true
