

# Tensor operators
#   On both tensor power and tensor product spaces
#       .to_matrix(), action, adj_action agree
#
#   Lift(A)
#   KroneckerProduct(A, B)
#   KroneckerProduct(A, B, C, D)
#
#   KroneckerSum(A, B)
#   KroneckerSum(A, B, C, D)


# Operator introspection
#
#   child_at
#       - empty returns root
#
#
#
#
#   with_exponentiator
#       -on root
#       -on leaf
#       -in between 
#
#   with_label
#       -on root
#       -on leaf
#       -in between 